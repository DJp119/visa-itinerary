---
const { title, description, className, children } = Astro.props;
---
<section class={`mb-12 ${className}`}>
  <h2 class="text-3xl font-bold text-foreground mb-4">
    {title}
  </h2>
  {description && <p class="text-lg text-foreground/60 mb-6">{description}</p>}
  {children}
</section>